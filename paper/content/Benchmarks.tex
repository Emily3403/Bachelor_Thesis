% "The integer value read from /dev/uioX represents the total interrupt count."
%   - can count missed interrupts


% For now: no kernel buffer
%   - in the future this could be a way of measuring how much overhead the double copy approach has

% How much performance is lost when disabling and enabling the interrupt registers all the time